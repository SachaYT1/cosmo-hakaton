"""Фильтрация техногенных термоаномалий (факелы, промплощадки) по повторяемости.

Идея из постановки: газовый факел неотличим от горения по одному снимку,
но повторяется из пролёта в пролёт, включая зимние месяцы, когда степь не горит.

ВАЖНО: на blind test это неприменимо напрямую — геопривязка и даты тестовых
чипов скрыты организаторами намеренно (см. постановку, "Тестовый набор
и обезличивание"), поэтому сопоставить тестовый чип с историческим архивом
по координатам нельзя. Эта функция — инструмент ПОДГОТОВКИ ОБУЧАЮЩИХ ДАННЫХ
(построение более чистых меток/доп. признака persistence по train-архиву),
а не шаг инференса. Модель должна научиться отличать факелы от природного
горения по признакам самого чипа (см. Рисунок 4 постановки: разность
детекций в одной точке, отражение в SWIR и т.д. — на train-выборке их можно
получить из meta/архива и подать как обучающий сигнал/доп. канал).
"""
from __future__ import annotations

import numpy as np


def filter_persistent_sources(
    pred_mask: np.ndarray,
    historical_hits: np.ndarray,
    min_winter_hits: int = 3,
) -> np.ndarray:
    """Обнуляет пиксели, которые исторически детектировались зимой >= min_winter_hits раз.

    historical_hits — заранее собранная по многолетнему архиву карта повторяемости
    детекций в данной точке (та же сетка, что и pred_mask).
    """
    raise NotImplementedError("TODO: пространственное сопоставление + маскирование")
